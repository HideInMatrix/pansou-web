---
title: ProxmoxVE通过Cloudflare Zero Trust实现内网穿透
id: cba4428b-7edf-4d29-bfff-13d81a1a74d2
date: 2025-05-30 20:46:38
auther: root
cover: 
excerpt: 前言 这个月偶然从网上看到了 Cloudflare Zero Trust这个东西。它大致相当于一个内网穿透工具，我之前将家里的服务器通过 nps 这个工具内网穿透出来，也很方便。但是购买的腾讯服务器是有时限的，为了防止服务器到期，我又嫌续费太贵无法继续续费，导致连不上家里的服务器了。所以想用 Clo
permalink: /archives/90f17ba13b3d
categories:
 - share
tags: 
 - proxmox
---

## 前言

这个月偶然从网上看到了 `Cloudflare Zero Trust`这个东西。它大致相当于一个内网穿透工具，我之前将家里的服务器通过 `nps` 这个工具内网穿透出来，也很方便。但是购买的腾讯服务器是有时限的，为了防止服务器到期，我又嫌续费太贵无法继续续费，导致连不上家里的服务器了。所以想用 `Cloudflare Zero Trust` 来做个内网穿透就当是备用方案了。

## 准备工作

1. Cloudflare 的账户（有国外的信用卡来注册 `Cloudflare Zero Trust` 账户）
2. 域名一个
3. 内网主机一台，系统是 `ProxmoxVE 7.4-3` (太低的版本我不知道具体会有什么差距)

请确保你有上面 3 个材料，不然无法制作一个内网穿透工具，前面 2 个是最重要的。

## 添加 Cloudflare Tunnels

这里是默认你已经注册好了上面需要的工具账户。登录进去的首页右侧 `Zero Trust` 进入，选择 `网络` 下面的 `Tunnels` ,点击 `tunnel` 创建一个隧道。

写入你的名字点击保存。跳到下面的界面上

![1e94e7b14c24e5b6656e8.png](https://pan.micromatrix.org/file/1e94e7b14c24e5b6656e8.png)

![53a5e2736f0a72f760866.png](https://pan.micromatrix.org/file/53a5e2736f0a72f760866.png)

选择对应的操作系统，复制粘贴它给你的命令，左侧是新安装的，右侧是已经安装过的。

运行命令之后返回到隧道列表页面，如果

![d35486294453eaab16039.png](https://pan.micromatrix.org/file/d35486294453eaab16039.png)

提示 `HEALTHY`则表示隧道打通了，反之则表明没有打通。

之后再次进入对应的隧道里，选择 `Public Hostname` 这里点击如下的按钮

![2339089f4437db1b3b1aa.png](https://pan.micromatrix.org/file/2339089f4437db1b3b1aa.png)

在接下来的页面中填入你的域名和你内部的服务器 IP:端口，这里你给的域名一定要没有在 CF 上的 DNS 绑定过

![48c8228310d4c80692a5a.png](https://pan.micromatrix.org/file/48c8228310d4c80692a5a.png)

由于我们是穿透 PVE 的网页控制端，所以按照我给的参数填写

![c4835cb3f1b9f520b789d.png](https://pan.micromatrix.org/file/c4835cb3f1b9f520b789d.png)

`No TLS Verify` 一定要勾选上。之后点击保存，就可以通过你设置的域名进行访问里。
